# --- BOT ---
FERNET_KEY=<<isi key Fernet 32-byte base64 yg kamu generate>>
MONGO_URI=<<string koneksi mongodb srv>>
MONGO_DB=soltrade
TRADE_SVC_URL=https://your-trade-svc.internal
TRADE_SVC_TOKEN=<<random-64-hex>>     # shared secret HMAC header

# --- TRADE-SVC ---
PORT=8080
RPC_URL=https://api.mainnet-beta.solana.com
JUP_API_KEY=                               # boleh kosong
AUTH_TOKEN=<<sama dengan TRADE_SVC_TOKEN>>
# === (opsional) platform-fee Jupiter ===
JUP_FEE_BPS=100                            # 1% = 100 bps
JUP_FEE_ACCOUNT=<<public key penampung fee>>
